import { FebonacciconverterPipe } from './fibonacciconverter.pipe';

describe('FebonacciconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new FebonacciconverterPipe();
    expect(pipe).toBeTruthy();
  });
});
